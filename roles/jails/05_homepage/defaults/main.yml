userland: "14.3-RELEASE"

pkg:
  - nginx
  - rsync
  - bash

files:
  - src: usr_local_etc_nginx_nginx.conf.j2
    dest: /usr/local/etc/nginx/nginx.conf
  - src: etc_crontab.j2
    dest: /etc/crontab

dirs:
  - /var/www

services:
  - nginx
